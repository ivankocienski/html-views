
* merge to master
* sub views 
* better tags
* html version
* CGI escaping?
* optimize (use of write over format)
* if layout is nil then just render raw html from view
* examples
* locals override in render
* alphabetize list-views
* remove demo.lisp and hacks.lisp
* readme

- package examples
- test on fresh instance



extras
======

- caching/pre-rendering?
- hueristic for not typing (h1 nil thing) but instead (h1 thing)?
- pretty printing html output?
- more examples
	- complex example
- render to a given stream (not just strings)


maybe
=====

- merge different tag lists?
